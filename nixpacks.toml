# Railway (Nixpacks) build config
# Banner Studio video render'ı için sistem Chromium'u — puppeteer-core bunu
# PATH'ten çözer (bkz. src/utils/bannerVideoRenderer.js resolveChromePath).
[phases.setup]
nixPkgs = ["...", "chromium"]
